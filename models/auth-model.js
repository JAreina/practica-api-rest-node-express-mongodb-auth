"use strict";

const conn = require("./auth-schema"); // conexion a mongo

class AuthModel{
    // si existe usuario
    getUser(user, cb){

    }
    setUser(user,cb){
        
    }
}

module.exports = AuthModel;